name: Release new version

on:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest
    steps:
      - name: Dump env
        run: env | sort     
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
        
